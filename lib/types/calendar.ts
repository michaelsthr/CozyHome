export interface EventInterface {
    id: string;
    name: string;
    startDate: string;
    endDate: string;
    repeat: boolean;
    creator: string;
    description: string;
    color: string;
    borderColor: string;
}
