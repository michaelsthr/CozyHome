import { StyleSheet } from "react-native";

export const fontStyles = StyleSheet.create({
    h1: {
        fontSize: 30,
        fontWeight: "bold",
        textAlign: "center",
        marginRight: "auto",
    },
});
